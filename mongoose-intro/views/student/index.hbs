{{#each students}}
<div>
    <span>Student ID: {{this.id}}</span>
    <strong>{{this.name}}</strong>
    <span>{{this.age}}</span>

    <a href="/students/{{this._id}}">View Student</a>
</div>
{{/each}}

<a href="/students/new">Add a New Student</a>